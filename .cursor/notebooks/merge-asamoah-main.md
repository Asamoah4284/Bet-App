
# Merge asamoah → main

## Done
Fetched `origin/asamoah` and merged into `main` (`fab63c8`), then pushed to GitHub.

## Conflicts resolved (kept both features)
- `backend/server.js` — shield + notifications + messages + subscription; listen on `0.0.0.0`
- `frontend/App.js` — fonts + auth session listener + Quibet splash timing
- `frontend/src/store/authStore.js` — push registration + subscription sync
- `frontend/src/services/api.js` — shield, notifications, and subscription APIs
- `SplashScreen.js` — Asamoah branded splash handoff
- `ReminderSettingsScreen.js` — Quibet copy + card spacing

## Asamoah features on main
Paywall, Moolre payments, subscription store/routes, native splash/brand assets, Quibet naming on buddy invite HTML.
